= render 'shared/messages', flash: flash
.ui.text.container#top
  h1.ui.header.centered I'm Tasketer!
  p.ui.header.centered Management task flexible
  br
  .ui.two.column.centered.grid
    .column
      = link_to 'Get Started!', new_user_registration_path, class: 'centered fluid massive ui button green'

.ui.vertical.stripe.segment
  .ui.middle.aligned.stackable.grid.container
    .row.top_segment
      .eight.wide.column
        .ui.middle.aligned.divided.list
          .item.content.top_list
            i.check.icon
            | add task
          .item.content.top_list
            i.check.icon
            | sort task by drag and drop
          .item.content.top_list
            i.check.icon
            | manage task in four segment
      .eight.wide.right.floated.column
        = image_tag('demo.mov.gif')
    .ui.divider
    .row.top_segment
      .eight.wide.column
        .ui.middle.aligned.divided.list
          .item.content.top_list
            i.check.icon
            | separate task by work space
      .eight.wide.right.floated.column
        = image_tag('ws_demo.mov.gif')
    .ui.divider
    .row.top_segment
      .eight.wide.column
        .ui.middle.aligned.divided.list
          .item.content.top_list
            i.check.icon
            | customize segment title
      .eight.wide.right.floated.column
        = image_tag('title_index.png')
    .row
      .eight.wide.column
        | priority matrix
        = image_tag('matrix_sample.png')
      .eight.wide.column
        | workflow
        = image_tag('flow_sample.png')
